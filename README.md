# 🚗 Car Rental Management System  
A full-stack rental system built with **FastAPI (Python)** for the backend and **React + Vite** for the frontend.  
Includes API documentation, autogenerated SDK support, and a PowerShell script for easy startup.

---

## ⭐ Features

### 🔧 Backend (FastAPI)
- Car CRUD operations  
- Rental management (create, list, cancel)  
- SQLite/PostgreSQL compatible  
- Auto-generated OpenAPI documentation  
- CORS configured for frontend  
- Easy SDK generation using openapi-generator  

### 🎨 Frontend (React + Vite)
- View all cars  
- Create rental  
- Cancel rental  
- Fully connected to backend REST API  
- Axios-based API communication  

### ▶️ Startup Automation
A `start.ps1` script launches both backend and frontend with a single command.

---

## 📂 Project Structure

```

Rental-System/
│
├── server/                # FastAPI backend
│   ├── main.py
│   ├── models.py
│   ├── database.py
│   ├── routers/
│   ├── run_server.bat
│   └── ...
│
├── client/                # React frontend
│   ├── src/
│   ├── vite.config.js
│   └── package.json
│
├── scripts/
│   ├── setup.ps1          # SDK generation + dependencies (optional)
│   ├── start.ps1          # Start backend & frontend
│   └── ...
│
└── README.md

````

---

# 🛠️ Installation Guide

## 🔹 1. Clone the Repository
```sh
git clone https://github.com/yourusername/Rental-System.git
cd Rental-System
````

---

# ⚙️ Backend Setup (FastAPI)

## 2️⃣ Create Virtual Environment

```sh
cd server
python -m venv venv
venv\Scripts\activate
```

## 3️⃣ Install Dependencies

```sh
pip install -r requirements.txt
```

If `requirements.txt` is missing, install manually:

```sh
pip install fastapi uvicorn sqlalchemy pydantic python-multipart
pip install fastapi-cors
```

## 4️⃣ Start Backend

```sh
uvicorn main:app --reload
```

Backend will run at:
👉 [http://127.0.0.1:8000](http://127.0.0.1:8000)
👉 API Docs: [http://127.0.0.1:8000/docs](http://127.0.0.1:8000/docs)

---

# 🎨 Frontend Setup (React + Vite)

## 1️⃣ Install Dependencies

```sh
cd ../client
npm install
```

## 2️⃣ Start Frontend

```sh
npm run dev
```

Frontend runs on:
👉 [http://localhost:5173](http://localhost:5173)

---

# 🏃 One-Step Startup (Recommended)

From project root:

```sh
.\start.ps1
```

This will:

* Start backend (FastAPI)
* Start frontend (Vite)
* Show logs in separate windows

---

# 🧰 Optional: Generate Python SDK from FastAPI

If you want to generate SDK:

## 1️⃣ Install OpenAPI Generator

```sh
npm install @openapitools/openapi-generator-cli -g
```

## 2️⃣ Run SDK Generation

```sh
openapi-generator-cli generate `
  -i http://127.0.0.1:8000/openapi.json `
  -g python `
  -o ./car_rental_sdk
```

SDK will appear in:

```
/car_rental_sdk
```

---

# 🚀 Deployment

You can deploy using:

* **Backend:** Render, Railway, AWS EC2, Docker
* **Frontend:** Netlify, Vercel, Cloudflare
* **Database:** PostgreSQL (recommended)

---

# 🧪 API Test Examples

## Get all cars:

```sh
GET http://127.0.0.1:8000/cars/
```

## Create rental:

```sh
POST http://127.0.0.1:8000/rentals/
```

## Cancel rental:

```sh
DELETE http://127.0.0.1:8000/rentals/{id}
```

---
